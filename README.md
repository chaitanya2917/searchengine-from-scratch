# searchengine-from-scratch
A fully functional search engine built from scratch in Python — featuring a web crawler, inverted index, TF-IDF ranking, PageRank algorithm, and a query interface. Core CS concepts implemented without using search libraries
